import { createTheme } from "@mui/material";

const Theme = createTheme({
  palette: {
    primary: {
      main: '#B9FBD8',
    },
    secondary: {
      main: '#fbb9dc',
    },
  }
})

export default Theme;